export type FormType = {
    firstName: string;
    lastName: string;
    email: string;
    password: string;
    confirmPassword: string;
}

export type NewData = {
    first_name: string;
    last_name: string;
    email: string;
    password: string;
};

export type FeedbackType = {
  variant?: string;
  message: string;
  title?: string;
};

